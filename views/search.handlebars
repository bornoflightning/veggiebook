<div id="signUpSection" class="d-flex flex-column align-items-center card loginBox gap-2">
    <h2 class="">Our search is under construction.</h2>
    <h2 class="">Please see results in your terminal window.</h2>
</div>

</div>
<div class="d-flex flex-row justify-content-around">
  <div class="d-flex card loginBox justify-content-around align-items-center gap-2">
      <div class="row">
          <h5 class="primaryFont">Click Here to Go Back</h5>
      </div>
      <div class="row">
          <button class="btn btn-dark primaryBtn loginBtn"><a href="/growerProfile" class="text-reset text-decoration-none">Profile</a></button>
      </div>
  </div>
</div>

{{!-- {{#each user as |user| }}
<div class="d-flex flex-row justify-content-around row">
    <div id="growerSection" class="d-flex flex-column align-items-center card loginBox gap-2 col-4">
        <h2 class="primaryFont">{{user.user_name}}</h2>
        <p class="form-control text-center gap-2">{{user.description}}</p>
    </div>
</div>
{{/each}} --}}
